# PostgreSQL de laboratorio

1. copiar `.env.example` a `.env`;
2. cambiar password;
3. `docker compose up -d`;
4. practicar `pg_dump` y restore;
5. destruir el laboratorio cuando termine.

No contiene datos reales.
