---
title: Mantener este Docusaurus
---

# Mantener la documentación de O.S.C.A.R.

El sitio está basado en `rudemex/railway-docusaurus-v3`, por lo que conserva el flujo clásico de Docusaurus v3 con TypeScript y Yarn, y suma un `Dockerfile` para Railway.

## Desarrollo local

```bash
yarn
yarn start
```

## Antes de subir cambios

```bash
yarn typecheck
yarn build
```

También existe:

```bash
yarn check
```

## Agregar una página

Crear un `.md` o `.mdx` dentro de `docs/`.

La sidebar está configurada como `autogenerated`, por lo que la posición y agrupación se controla principalmente con la estructura de carpetas, `_category_.json` y el front matter.

## Agregar una entrada de bitácora

Crear un archivo dentro de `blog/` con fecha y slug:

```text
blog/2026-09-01-nuevo-switch.md
```

Usar la bitácora para registrar **qué cambió y por qué**. La guía en `docs/` debe explicar **cómo funciona y cómo operarlo**.

## Railway

El `Dockerfile` construye el sitio estático y lo sirve usando el puerto indicado por `PORT`.

Configurar en Railway:

```text
SITE_URL=https://docs-oscar.example.com
```

Si todavía no hay dominio definitivo, el sitio funciona igualmente; `SITE_URL` afecta principalmente URLs absolutas, metadata y canonical links.

## Troubleshooting del build

**`yarn build` falla con `RealContentHashPlugin` / "Some kind of unexpected caching problem occurred"** — es caché de webpack corrupta (típico tras un build interrumpido a la mitad, o al cambiar de rama sin limpiar), no un problema del contenido. Solución:

```bash
yarn clear   # borra build/, .docusaurus/ y node_modules/.cache
yarn build
```

Si persiste, borrar `node_modules/.cache` a mano (a veces `docusaurus clear` no lo limpia si el proceso anterior murió a mitad de escritura) y reintentar.

**`yarn typecheck` falla con `Cannot find module '@site/...'`** — el `tsconfig.json` del proyecto debe declarar `"baseUrl": "."` explícitamente; heredarlo solo de `@docusaurus/tsconfig` no alcanza, porque TypeScript resuelve `baseUrl` relativo a dónde se *define*, no a dónde se hereda.

## Dependencias

Dependabot revisa el ecosistema npm, lo cual también cubre dependencias gestionadas con Yarn.

Cuando se actualice Docusaurus, hacerlo en bloque para que `@docusaurus/core`, `preset-classic`, `theme-mermaid`, aliases, tsconfig y types permanezcan en la misma versión.
